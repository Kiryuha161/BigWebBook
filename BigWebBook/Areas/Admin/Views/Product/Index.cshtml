@model List<Product>



    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">Список книг</h2>
        </div>

        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Создать продукт</a>
        </div>
    </div>

    <table id="tableData" class="table table-bordered table-striped table-hover" style="width: 99%">
        <thead>
            <tr>
                <th>Название</th>
                <th>Изображение</th>
                <th>Автор</th>
                <th>Описание</th>
                <th>Жанр</th>
                <th>ISBN</th>
                <th>Прейскурант</th>
                <th>1-50</th>
                <th>50+</th>
                <th>100+</th>
                <th></th>
            </tr>
        </thead>
       @* <tbody>
            @foreach (var product in Model.OrderBy(u => u.Id))
            {
                <tr class="table-primary">
                    <td>@product.Id</td>
                    <td>@product.Title</td>
                    <td><img src="@product.ImageUrl" style="width: 150px; height: 150px"/></td>
                    <td>@product.Author</td>
                    <td>@Html.Raw(product.Description)</td>
                    <td>@product.CategoryName.Name</td>
                    <td>@product.ISBN</td>
                    <td>@product.ListPrice</td>
                    <td>@product.Price</td>
                    <td>@product.Price50</td>
                    <td>@product.Price100</td>
                    <td>
                        <div class="w-100 btn-group" role="group">
                            <a asp-controller="Product" asp-action="Upsert" asp-route-id="@product.Id" class=" w-50 btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Редактировать</a>
                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@product.Id" class="w-50 btn btn-danger mx-2"><i class="bi bi-trash3-fill"></i> Удалить</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>*@
    </table>

    @section Scripts {
        <script src="~/js/product.js"></script>
    }

